# Как запускать main_pose_tracker.py и main.py вместе

## 📊 Текущая ситуация

### main_pose_tracker.py
- **UDP порт:** `5000` (жестко задан в коде)
- **Формат данных:** Только углы позы
- **Функции:** GUI, отслеживание позы, NDI, виртуальная камера
- **Трекинг штанги:** ❌ Нет

### main.py
- **UDP порт:** `5005` (из config.py)
- **Формат данных:** Поза + штанга + путь + все joints
- **Функции:** Консольный интерфейс, отслеживание позы + штанги
- **Трекинг штанги:** ✅ Да

## 🔄 Варианты запуска

### Вариант 1: Запуск обоих параллельно (НЕ рекомендуется)

**Проблемы:**
- Два процесса обрабатывают одно и то же видео → двойная нагрузка на CPU/GPU
- Разные UDP порты → нужно два получателя
- Дублирование обработки позы

**Как запустить:**
```bash
# Терминал 1: GUI для позы
python main_pose_tracker.py

# Терминал 2: Консольный для штанги
python main.py
```

**Когда использовать:**
- Только для тестирования
- Если нужны разные форматы данных на разных портах

---

### Вариант 2: Использовать только main.py (рекомендуется для трекинга штанги)

**Преимущества:**
- Один процесс → меньше нагрузка
- Все данные в одном формате
- Трекинг позы + штанги

**Как запустить:**
```bash
python main.py
# или с видео файлом:
python main.py -v vids/your_video.mp4
```

**Горячие клавиши:**
- `q` - выход
- `c` - очистить путь штанги
- `1` - отключить трекинг тела
- `l` - переключить трекинг ног
- `d` - режим отладки
- `w/s` - настройка param2
- И другие (см. вывод при запуске)

---

### Вариант 3: Использовать только main_pose_tracker.py (если не нужна штанга)

**Преимущества:**
- GUI для удобной настройки
- NDI и виртуальная камера
- Настройка внешнего вида скелета

**Как запустить:**
```bash
python main_pose_tracker.py
```

**Ограничения:**
- Нет трекинга штанги
- Порт 5000 (не из config.py)

---

### Вариант 4: Объединенная версия (ЛУЧШЕЕ РЕШЕНИЕ) ⭐

Создать `unified_main.py` который объединяет:
- ✅ GUI из main_pose_tracker.py
- ✅ Трекинг штанги из main.py
- ✅ Единый формат UDP данных
- ✅ Использование config.py

**Как запустить:**
```bash
python unified_main.py
```

---

## 🎯 Рекомендация

**Для работы со штангой:** Используйте `main.py` или создайте объединенную версию.

**Только для позы с GUI:** Используйте `main_pose_tracker.py`.

**НЕ запускайте оба одновременно** - это неэффективно.

## 🔧 Настройка портов

Если нужно изменить порты:

### main_pose_tracker.py:
Измените строку 39:
```python
UE_IP, UE_PORT = "127.0.0.1", 5000  # Измените на нужный порт
```

### main.py:
Измените в `config.py`:
```python
UDP_PORT = 5005  # Измените на нужный порт
```

## 📡 Формат UDP данных

### main_pose_tracker.py (порт 5000):
```json
{
  "pose_detected": true,
  "angles": {
    "left_arm": 120.5,
    "right_arm": 125.3,
    "left_leg": 90.0,
    "right_leg": 95.2
  }
}
```

### main.py (порт 5005):
```json
{
  "timestamp": 1234567890.123,
  "barbell": {
    "position": [640, 360],
    "confidence": 0.9,
    "source": "hough"
  },
  "knee_positions": {
    "left_knee": [300, 400],
    "right_knee": [340, 400],
    "left_knee_angle": 90.0,
    "right_knee_angle": 95.0
  },
  "joints": {...},
  "barbell_path": [...]
}
```

## ⚠️ Важные замечания

1. **Оба используют одну камеру** - если запускаете параллельно, один из них может не получить доступ к камере
2. **Разные порты** - получатель должен слушать оба порта (5000 и 5005)
3. **Двойная нагрузка** - MediaPipe обрабатывается дважды
4. **Разные форматы** - нужно два парсера на стороне получателя

## ✅ Итоговая рекомендация

**Создайте объединенную версию `unified_main.py`** - это лучшее решение, которое:
- Объединяет все функции
- Использует один процесс
- Отправляет данные в едином формате
- Имеет GUI для удобства

Хотите, чтобы я создал `unified_main.py`?

