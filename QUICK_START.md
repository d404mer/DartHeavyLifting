# 🚀 Быстрый старт - Как правильно запускать

## ⚠️ Важно: НЕ запускайте оба файла одновременно!

Они используют одну камеру и будут конфликтовать.

---

## 📋 Варианты использования

### ✅ Вариант 1: Только трекинг позы (GUI) - `main_pose_tracker.py`

**Когда использовать:**
- Нужен графический интерфейс для настройки
- Нужны NDI или виртуальная камера
- НЕ нужен трекинг штанги

**Запуск:**
```bash
python main_pose_tracker.py
```

**Что делает:**
- ✅ Открывает GUI окно
- ✅ Отслеживает позу человека
- ✅ Вычисляет углы рук и ног
- ✅ Отправляет данные на UDP порт (из config.py или 5000)
- ❌ НЕ отслеживает штангу

---

### ✅ Вариант 2: Трекинг позы + штанги (консоль) - `main.py`

**Когда использовать:**
- Нужен трекинг штанги
- Нужны все данные (поза + штанга + путь)
- GUI не нужен

**Запуск:**
```bash
# С камеры
python main.py

# С видео файла
python main.py -v vids/your_video.mp4
```

**Что делает:**
- ✅ Отслеживает позу человека
- ✅ Отслеживает штангу
- ✅ Отправляет полные данные на UDP порт (из config.py, по умолчанию 5005)
- ✅ Визуализация пути штанги
- ❌ Нет GUI (только консоль)

**Горячие клавиши:**
- `q` - выход
- `c` - очистить путь штанги
- `1` - отключить трекинг тела
- `l` - переключить трекинг ног
- `d` - режим отладки штанги
- `w/s` - настройка param2 (основной параметр обнаружения)
- И другие (см. вывод при запуске)

---

## 🔧 Настройка портов

### main_pose_tracker.py
Теперь использует `config.py` (если доступен), иначе порт 5000.

### main.py
Использует `config.py`:
```python
UDP_PORT = 5005  # Измените в config.py
```

---

## 📊 Формат UDP данных

### main_pose_tracker.py:
```json
{
  "pose_detected": true,
  "angles": {
    "left_arm": 120.5,
    "right_arm": 125.3,
    "left_leg": 90.0,
    "right_leg": 95.2
  }
}
```

### main.py (полный формат):
```json
{
  "timestamp": 1234567890.123,
  "barbell": {
    "position": [640, 360],
    "confidence": 0.9,
    "source": "hough"
  },
  "knee_positions": {
    "left_knee": [300, 400],
    "right_knee": [340, 400],
    "left_knee_angle": 90.0,
    "right_knee_angle": 95.0
  },
  "joints": {...},
  "barbell_path": [...]
}
```

---

## 🎯 Рекомендация

**Для работы со штангой:** Используйте `main.py`

**Только для позы с GUI:** Используйте `main_pose_tracker.py`

**НЕ запускайте оба одновременно** - это неэффективно и может вызвать конфликты с камерой.

## 📷 Использование разных камер

### main.py:
```bash
# Камера 0 (по умолчанию)
python main.py

# Камера 1
python main.py -c 1

# Камера 2
python main.py -c 2
```

### main_pose_tracker.py:
Выберите камеру в GUI из списка доступных камер.

### Запуск двух процессов с разными камерами:
```bash
# Терминал 1: Камера 0
python main.py -c 0

# Терминал 2: Камера 1  
python main.py -c 1
```

**Важно:** Каждый процесс использует свою камеру, поэтому можно запускать несколько одновременно с разными камерами!

---

## ❓ Частые вопросы

**Q: Можно ли запустить оба одновременно?**  
A: Технически да, но НЕ рекомендуется:
- Двойная нагрузка на CPU/GPU
- Конфликт доступа к камере
- Разные порты UDP
- Дублирование обработки

**Q: Какой файл использовать?**  
A: 
- Нужна штанга → `main.py`
- Нужен GUI и нет штанги → `main_pose_tracker.py`

**Q: Как изменить UDP порт?**  
A: Измените `UDP_PORT` в `config.py` (для обоих файлов)

**Q: Можно ли добавить GUI к main.py?**  
A: Да, но это требует объединения кода. Сейчас используйте `main_pose_tracker.py` для GUI.

