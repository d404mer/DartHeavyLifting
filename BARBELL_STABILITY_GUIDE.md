# Руководство по настройке стабильности детектирования штанги

## Проблема: "скачет с круга на круг"

Если детектирование переключается между разными окружностями, используйте следующие параметры для настройки.

---

## 🔧 Параметры для предотвращения переключений между кругами

### 1. **BARBELL_CIRCLE_MIN_DIST** (в `config.py`)
**Что делает:** Минимальное расстояние между центрами окружностей в пикселях.

**Как настраивать:**
- **Увеличьте** (например, 70-100) → находит меньше окружностей → меньше вариантов для переключения
- **Уменьшите** (например, 30-40) → находит больше окружностей (если штанга не находится)

**Горячие клавиши:** `o` (уменьшить) / `p` (увеличить)

**Рекомендация:** Начните с 80, если переключения продолжаются - увеличьте до 100-120.

---

### 2. **BARBELL_CIRCLE_PARAM2** (в `config.py`)
**Что делает:** Порог аккумулятора для обнаружения окружностей (основной параметр).

**Как настраивать:**
- **Увеличьте** (например, 40-50) → более строгое детектирование → находит меньше окружностей
- **Уменьшите** (например, 20-30) → более мягкое детектирование → находит больше окружностей

**Горячие клавиши:** `w` (увеличить) / `s` (уменьшить)

**Рекомендация:** Если переключения продолжаются, увеличьте до 45-50.

---

### 3. **BARBELL_POSITION_STABILITY_WEIGHT** (в `config.py`)
**Что делает:** Вес стабильности позиции при выборе окружности (0.0-1.0).

**Как настраивать:**
- **Увеличьте** (например, 0.8-0.9) → сильнее предпочитает окружности близкие к предыдущей позиции
- **Уменьшите** (например, 0.5-0.6) → меньше приоритет стабильности позиции

**Рекомендация:** Начните с 0.8, если переключения продолжаются - увеличьте до 0.9.

**Важно:** Сумма `BARBELL_POSITION_STABILITY_WEIGHT + BARBELL_RADIUS_STABILITY_WEIGHT` не должна превышать 1.0!

---

### 4. **BARBELL_RADIUS_STABILITY_WEIGHT** (в `config.py`)
**Что делает:** Вес стабильности радиуса при выборе окружности (0.0-1.0).

**Как настраивать:**
- **Увеличьте** (например, 0.4-0.5) → сильнее предпочитает окружности с похожим радиусом
- **Уменьшите** (например, 0.1-0.2) → меньше приоритет стабильности радиуса

**Рекомендация:** Если радиус штанги меняется, уменьшите до 0.1-0.2. Если радиус стабилен, увеличьте до 0.4-0.5.

---

### 5. **BARBELL_X_STABILITY_GAIN** (в `config.py`)
**Что делает:** Усиление штрафа за горизонтальные скачки при выборе окружности.

**Как настраивать:**
- **Увеличьте** (например, 2.0-3.0) → сильнее штрафует горизонтальные переключения
- **Уменьшите** (например, 1.0-1.5) → меньше штраф за горизонтальные скачки

**Рекомендация:** Если переключения происходят по горизонтали, увеличьте до 2.5-3.0.

---

### 6. **BARBELL_REJECT_OUTLIERS** (в `config.py`)
**Что делает:** Отбрасывать резкие скачки по положению/радиусу.

**Как настраивать:**
- **True** → отбрасывает выбросы (резкие скачки) → более стабильно, но может добавить задержку
- **False** → не отбрасывает выбросы → менее стабильно, но без задержки

**Рекомендация:** Включите (`True`), если переключения резкие и неожиданные.

---

### 7. **BARBELL_MAX_JUMP_PIXELS** (в `config.py`)
**Что делает:** Максимально допустимый скачок центра между кадрами (работает только если `BARBELL_REJECT_OUTLIERS = True`).

**Как настраивать:**
- **Уменьшите** (например, 60-80) → более строгая фильтрация → отбрасывает больше скачков
- **Увеличьте** (например, 150-200) → более мягкая фильтрация → отбрасывает меньше скачков

**Рекомендация:** Начните с 80, если штанга движется быстро - увеличьте до 100-120.

---

## 🎯 Пошаговая настройка

### Шаг 1: Уменьшите количество найденных окружностей
1. Откройте `config.py`
2. Увеличьте `BARBELL_CIRCLE_MIN_DIST` до 80-100
3. Увеличьте `BARBELL_CIRCLE_PARAM2` до 45-50
4. Запустите программу и проверьте

### Шаг 2: Усильте приоритет стабильности позиции
1. В `config.py` увеличьте `BARBELL_POSITION_STABILITY_WEIGHT` до 0.8-0.9
2. Уменьшите `BARBELL_RADIUS_STABILITY_WEIGHT` до 0.1-0.2 (чтобы сумма не превышала 1.0)
3. Проверьте результат

### Шаг 3: Включите фильтрацию выбросов
1. В `config.py` установите `BARBELL_REJECT_OUTLIERS = True`
2. Установите `BARBELL_MAX_JUMP_PIXELS = 80`
3. Проверьте результат

### Шаг 4: Усильте штраф за горизонтальные скачки
1. В `config.py` увеличьте `BARBELL_X_STABILITY_GAIN` до 2.5-3.0
2. Проверьте результат

---

## 🔍 Диагностика

### Включите режим отладки
1. Запустите программу
2. Нажмите клавишу `d` для включения режима отладки
3. Вы увидите:
   - **Желтый прямоугольник** - область поиска (между руками)
   - **Желтые окружности** - все найденные окружности
   - **Черно-белый прямоугольник** - область после предобработки

### Что смотреть:
- Если видите **много желтых окружностей** → увеличьте `BARBELL_CIRCLE_MIN_DIST` и `BARBELL_CIRCLE_PARAM2`
- Если окружности **далеко друг от друга** → переключения должны прекратиться
- Если окружности **близко друг к другу** → увеличьте `BARBELL_CIRCLE_MIN_DIST`

---

## ⚡ Быстрая настройка через горячие клавиши (во время работы)

| Клавиша | Параметр | Действие |
|---------|----------|----------|
| `w` | `BARBELL_CIRCLE_PARAM2` | Увеличить (строже детектирование) |
| `s` | `BARBELL_CIRCLE_PARAM2` | Уменьшить (мягче детектирование) |
| `p` | `BARBELL_CIRCLE_MIN_DIST` | Увеличить (меньше окружностей) |
| `o` | `BARBELL_CIRCLE_MIN_DIST` | Уменьшить (больше окружностей) |
| `j` | `BARBELL_BLUR_SIZE/SIGMA` | Увеличить размытие |
| `k` | `BARBELL_BLUR_SIZE/SIGMA` | Уменьшить размытие |
| `d` | `BARBELL_DEBUG_MODE` | Переключить режим отладки |

---

## 📝 Пример конфигурации для стабильного детектирования

```python
# В config.py

# Уменьшить количество найденных окружностей
BARBELL_CIRCLE_MIN_DIST = 80
BARBELL_CIRCLE_PARAM2 = 45

# Усилить приоритет стабильности позиции
BARBELL_POSITION_STABILITY_WEIGHT = 0.8
BARBELL_RADIUS_STABILITY_WEIGHT = 0.2

# Усилить штраф за горизонтальные скачки
BARBELL_X_STABILITY_GAIN = 2.5

# Включить фильтрацию выбросов
BARBELL_REJECT_OUTLIERS = True
BARBELL_MAX_JUMP_PIXELS = 80
```

---

## ❓ Частые вопросы

**Q: Штанга не находится после увеличения параметров**
A: Уменьшите `BARBELL_CIRCLE_PARAM2` (клавиша `s`) и `BARBELL_CIRCLE_MIN_DIST` (клавиша `o`)

**Q: Переключения продолжаются**
A: Увеличьте `BARBELL_POSITION_STABILITY_WEIGHT` до 0.9 и `BARBELL_X_STABILITY_GAIN` до 3.0

**Q: Траектория стала "залипать"**
A: Уменьшите `BARBELL_POSITION_STABILITY_WEIGHT` до 0.6-0.7

**Q: Как понять, что настройка правильная?**
A: Включите режим отладки (`d`) и смотрите, сколько окружностей находится. Должна находиться одна основная окружность (штанга).

