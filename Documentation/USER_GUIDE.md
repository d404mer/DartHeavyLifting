# Полное руководство пользователя DartHeavyLifting

## Оглавление

1. [Введение](#введение)
2. [Установка](#установка)
3. [Быстрый старт](#быстрый-старт)
4. [Основные функции](#основные-функции)
5. [Интерфейс приложения](#интерфейс-приложения)
6. [Режимы работы](#режимы-работы)
7. [Настройка трекинга штанги](#настройка-трекинга-штанги)
8. [Настройка трекинга позы](#настройка-трекинга-позы)
9. [UDP данные и интеграция](#udp-данные-и-интеграция)
10. [Дополнительные возможности](#дополнительные-возможности)
11. [Решение проблем](#решение-проблем)
12. [Горячие клавиши](#горячие-клавиши)
13. [Технические детали](#технические-детали)

---

## Введение

### Что такое DartHeavyLifting?

**DartHeavyLifting** — это профессиональное приложение для отслеживания позы спортсмена и траектории движения штанги в реальном времени. Приложение предназначено для анализа техники выполнения упражнений, интеграции с системами визуализации (Unreal Engine, OBS и др.) и создания интерактивных тренировочных программ.

![Главный экран приложения](screenshots/main_screen.png)
*Главное окно приложения с визуализацией позы и траектории штанги*

### Основные возможности

- ✅ **Отслеживание позы человека** через MediaPipe Pose (33 ключевые точки)
- ✅ **Обнаружение и трекинг штанги** по окружности (HoughCircles) с оптимизированным алгоритмом
- ✅ **Расчет углов суставов** (колени, локти, плечи, бедра)
- ✅ **Визуализация в реальном времени**: скелет, траектория штанги, углы
- ✅ **UDP стриминг данных** в формате JSON для интеграции с внешними системами
- ✅ **Графический интерфейс** для удобной настройки всех параметров
- ✅ **Поддержка NDI и виртуальной камеры** для стриминга
- ✅ **Поддержка DeckLink карт** через FFmpeg для профессионального видео

### Системные требования

- **Операционная система**: Windows 10/11, Linux, macOS
- **Python**: 3.8, 3.9, 3.10 или 3.11 (рекомендуется 3.10)
  - ⚠️ **Важно**: Python 3.12+ не поддерживается MediaPipe!
- **ОЗУ**: минимум 4 ГБ (рекомендуется 8 ГБ)
- **Видеокарта**: необязательно, но рекомендуется для лучшей производительности
- **Камера**: USB-камера или карта захвата (DeckLink, Blackmagic и др.)

---

## Установка

### Шаг 1: Проверка версии Python

Перед установкой убедитесь, что у вас установлена правильная версия Python:

```bash
python --version
```

Если версия Python 3.12 или выше, необходимо установить Python 3.10.

![Проверка версии Python](screenshots/python_version_check.png)
*Проверка версии Python в командной строке*

### Шаг 2: Установка Python 3.10 (если требуется)

#### Windows

**Вариант A: Автоматическая установка (рекомендуется)**

Запустите скрипт установки:

```bash
setup_python310.bat
```

Затем настройте проект:

```bash
quick_setup.bat
```

**Вариант B: Через winget**

```bash
winget install Python.Python.3.10
py -3.10 -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

**Вариант C: Через Python Launcher**

```bash
py -3.10 -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

#### Linux/macOS

Используйте pyenv или conda:

```bash
# Через pyenv
pyenv install 3.10.12
pyenv local 3.10.12

# Через conda
conda create -n dartlifting python=3.10
conda activate dartlifting
pip install -r requirements.txt
```

### Шаг 3: Установка зависимостей

После активации виртуального окружения установите все необходимые пакеты:

```bash
# Активируйте виртуальное окружение
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/macOS

# Установите зависимости
pip install -r requirements.txt
```

![Установка зависимостей](screenshots/install_dependencies.png)
*Процесс установки зависимостей*

### Шаг 4: Проверка установки

Проверьте, что все модули установлены корректно:

```bash
python check_python.py
```

Должно вывести сообщение об успешной установке всех модулей.

### Решение проблем при установке

#### Проблема: MediaPipe не устанавливается

**Решение 1**: Убедитесь, что используется Python 3.8-3.11:

```bash
python --version
```

**Решение 2**: Обновите pip:

```bash
python -m pip install --upgrade pip
```

**Решение 3**: Установите MediaPipe отдельно:

```bash
pip install mediapipe==0.10.9
```

#### Проблема: Ошибки компиляции на Windows

Установите Microsoft Visual C++ Redistributable:
- Скачайте с официального сайта Microsoft
- Установите последнюю версию

---

## Быстрый старт

### Первый запуск

1. **Активируйте виртуальное окружение**:

```bash
venv\Scripts\activate  # Windows
```

2. **Запустите приложение**:

```bash
python unified_main.py
```

![Первый запуск](screenshots/first_launch.png)
*Окно приложения при первом запуске*

### Базовая настройка

1. **Выберите источник видео**:
   - В секции "📷 Источник видео" выберите камеру из списка
   - Или выберите видео файл для тестирования

2. **Выберите режим работы**:
   - **Поза + штанга** (рекомендуется) — полный функционал
   - **Только поза** — только отслеживание позы
   - **Только штанга** — только отслеживание штанги

3. **Нажмите "▶️ Запуск"**

![Базовая настройка](screenshots/basic_setup.png)
*Интерфейс настройки перед запуском*

### Быстрый тест с видео файлом

Для быстрого тестирования без камеры:

```bash
python unified_main.py
```

Затем в GUI выберите "Видео файл" и укажите путь к тестовому видео.

Или через командную строку (для `main.py`):

```bash
python main.py -v "путь/к/видео.mp4"
```

---

## Основные функции

### Отслеживание позы

Приложение отслеживает 33 ключевые точки тела человека через MediaPipe Pose:

- **Голова**: нос, глаза, уши
- **Туловище**: плечи, грудь, таз
- **Руки**: плечи, локти, запястья, пальцы
- **Ноги**: бедра, колени, лодыжки, ступни

![Ключевые точки позы](screenshots/pose_landmarks.png)
*33 ключевые точки MediaPipe Pose*

**Вычисляемые углы**:
- Углы коленей (левое/правое)
- Углы локтей (левое/правое)
- Углы бедер (левое/правое)
- Углы плеч (левое/правое)

### Отслеживание штанги

Приложение обнаруживает штангу по окружности (вид сбоку) и отслеживает её траекторию:

- **Обнаружение**: алгоритм HoughCircles для поиска окружностей
- **Трекинг**: оптимизированный алгоритм с анти-дрожанием
- **Визуализация**: путь движения штанги в реальном времени
- **Авто-очистка**: автоматическая очистка пути при простое

![Траектория штанги](screenshots/barbell_tracking.png)
*Визуализация траектории движения штанги*

### Визуализация

Приложение отображает:

- **Скелет человека**: кости и суставы с цветовой кодировкой
- **Траектория штанги**: зеленая линия пути движения
- **Углы суставов**: отображаются рядом с соответствующими суставами
- **Текущая позиция штанги**: красная точка

![Полная визуализация](screenshots/full_visualization.png)
*Полная визуализация с позой и траекторией штанги*

---

## Интерфейс приложения

### Главное окно

Главное окно приложения разделено на несколько секций:

![Структура интерфейса](screenshots/interface_structure.png)
*Структура главного окна приложения*

#### 1. Режим работы

Выберите, что отслеживать:

- **Только поза** — отслеживание только позы человека
- **Только штанга** — отслеживание только штанги
- **Поза + штанга** — полный функционал (рекомендуется)

![Выбор режима](screenshots/mode_selection.png)
*Выбор режима работы*

#### 2. Источник видео

- **Камера**: выберите камеру из списка доступных
- **Видео файл**: выберите MP4/MOV/AVI файл для тестирования
- **Обновить камеры**: обновить список доступных камер

![Выбор источника](screenshots/video_source.png)
*Выбор источника видео*

#### 3. Настройки обработки

- **Разрешение обработки**: влияет на скорость (меньше = быстрее)
- **Частота кадров**: целевой FPS
- **Обработка каждого N-го кадра**: пропуск кадров для производительности

![Настройки обработки](screenshots/processing_settings.png)
*Настройки обработки видео*

#### 4. Внешний вид

Настройте визуализацию:

- **Показывать суставы**: включить/выключить отображение скелета
- **Цвет костей**: цвет линий скелета
- **Цвет суставов**: цвет точек суставов
- **Толщина костей**: толщина линий
- **Радиус суставов**: размер точек суставов
- **Размер шрифта**: размер текста углов
- **Толщина шрифта**: толщина обводки текста

![Настройки внешнего вида](screenshots/appearance_settings.png)
*Настройки внешнего вида визуализации*

#### 5. Настройки модели

Параметры MediaPipe Pose:

- **Сложность модели**: 0 (быстро) — 2 (точно)
- **Сглаживание landmarks**: включить/выключить
- **Минимальная уверенность обнаружения**: порог для обнаружения позы
- **Минимальная уверенность отслеживания**: порог для отслеживания

![Настройки модели](screenshots/model_settings.png)
*Настройки модели MediaPipe*

#### 6. Выходные потоки

Дополнительные выходы:

- **NDI**: включить стриминг через NDI
- **Виртуальная камера**: включить виртуальную камеру
- **Имя NDI потока**: имя для NDI стрима

![Выходные потоки](screenshots/output_streams.png)
*Настройки выходных потоков*

#### 7. Кнопки управления

- **▶️ Запуск**: начать обработку видео
- **⏹️ Остановка**: остановить обработку
- **❌ Выход**: закрыть приложение

![Кнопки управления](screenshots/control_buttons.png)
*Кнопки управления приложением*

#### 8. Предпросмотр

Миниатюра текущего кадра с наложенной визуализацией (обновляется с частотой 30 FPS).

![Предпросмотр](screenshots/preview.png)
*Предпросмотр видео с визуализацией*

---

## Режимы работы

### Режим "Только поза"

**Когда использовать**:
- Нужен только анализ позы спортсмена
- Не требуется отслеживание штанги
- Максимальная производительность для трекинга позы

**Что отслеживается**:
- ✅ 33 ключевые точки тела
- ✅ Углы всех суставов
- ✅ Координаты суставов
- ❌ Штанга не отслеживается

**UDP данные**:
```json
{
  "timestamp": 1234567890.123,
  "knee_positions": {...},
  "joints": {...},
  "angles": {...}
}
```

### Режим "Только штанга"

**Когда использовать**:
- Нужен только анализ траектории штанги
- Не требуется отслеживание позы
- Максимальная производительность для трекинга штанги

**Что отслеживается**:
- ✅ Позиция штанги
- ✅ Траектория движения
- ✅ Уверенность обнаружения
- ❌ Поза не отслеживается

**UDP данные**:
```json
{
  "timestamp": 1234567890.123,
  "barbell": {
    "position": [640, 360],
    "confidence": 0.9,
    "source": "hough"
  },
  "barbell_path": [...]
}
```

### Режим "Поза + штанга" (рекомендуется)

**Когда использовать**:
- Нужен полный анализ техники упражнения
- Интеграция с Unreal Engine или другими системами
- Создание комплексных тренировочных программ

**Что отслеживается**:
- ✅ 33 ключевые точки тела
- ✅ Углы всех суставов
- ✅ Позиция штанги
- ✅ Траектория движения штанги
- ✅ Использование позиции рук для улучшения поиска штанги

**UDP данные**:
```json
{
  "timestamp": 1234567890.123,
  "barbell": {...},
  "knee_positions": {...},
  "joints": {...},
  "barbell_path": [...],
  "angles": {...}
}
```

![Режим поза + штанга](screenshots/mode_both.png)
*Работа в режиме "Поза + штанга"*

---

## Настройка трекинга штанги

### Основные параметры

#### Размер окружности штанги

**BARBELL_CIRCLE_MIN_RADIUS** и **BARBELL_CIRCLE_MAX_RADIUS**

Определяют диапазон размеров окружности штанги в пикселях.

**Как настроить**:
- Если штанга **большая** (близко к камере): увеличьте `MAX_RADIUS` до 100-150
- Если штанга **маленькая** (далеко от камеры): уменьшите `MIN_RADIUS` до 20-30

**Горячие клавиши**: `t` (уменьшить minRadius) / `y` (увеличить minRadius), `u` (уменьшить maxRadius) / `i` (увеличить maxRadius)

![Настройка размера окружности](screenshots/barbell_radius_setting.png)
*Настройка размера окружности штанги*

#### Порог обнаружения (param2)

**BARBELL_CIRCLE_PARAM2**

Основной параметр для настройки обнаружения окружностей (10-50).

**Как настроить**:
- **Увеличить** (40-50): строже обнаружение, меньше ложных срабатываний
- **Уменьшить** (20-30): мягче обнаружение, находит больше окружностей

**Горячие клавиши**: `w` (увеличить) / `s` (уменьшить)

**Рекомендация**: Начните с 30 и уменьшайте (`s`), если штанга не находится.

![Настройка param2](screenshots/param2_setting.png)
*Настройка порога обнаружения*

#### Минимальное расстояние между окружностями

**BARBELL_CIRCLE_MIN_DIST**

Минимальное расстояние между центрами окружностей в пикселях.

**Как настроить**:
- **Увеличить** (80-100): окружности должны быть дальше друг от друга (меньше ложных срабатываний)
- **Уменьшить** (30-40): окружности могут быть ближе (больше вариантов)

**Горячие клавиши**: `o` (уменьшить) / `p` (увеличить)

**Рекомендация**: Начните с 50, увеличьте до 80-100, если переключается между разными окружностями.

### Режим отладки

Включите режим отладки для визуализации процесса обнаружения:

**Включение**: Нажмите клавишу `d` или установите `BARBELL_DEBUG_MODE = True` в `config.py`

**Что показывает**:
- **Желтый прямоугольник**: область поиска (между руками)
- **Желтые окружности**: все найденные окружности
- **Зеленая окружность**: выбранная штанга
- **Черно-белый прямоугольник**: область после предобработки

![Режим отладки](screenshots/debug_mode.png)
*Режим отладки с визуализацией процесса обнаружения*

### Настройка стабильности

Если детектирование "скачет" между разными окружностями:

#### 1. Уменьшите количество найденных окружностей

```python
# В config.py
BARBELL_CIRCLE_MIN_DIST = 80  # Увеличить
BARBELL_CIRCLE_PARAM2 = 45    # Увеличить
```

#### 2. Усильте приоритет стабильности позиции

```python
# В config.py
BARBELL_POSITION_STABILITY_WEIGHT = 0.8  # Увеличить
BARBELL_RADIUS_STABILITY_WEIGHT = 0.2    # Уменьшить
```

**Важно**: Сумма `POSITION_STABILITY_WEIGHT + RADIUS_STABILITY_WEIGHT` не должна превышать 1.0!

#### 3. Включите фильтрацию выбросов

```python
# В config.py
BARBELL_REJECT_OUTLIERS = True
BARBELL_MAX_JUMP_PIXELS = 80
```

#### 4. Усильте штраф за горизонтальные скачки

```python
# В config.py
BARBELL_X_STABILITY_GAIN = 2.5  # Увеличить до 2.5-3.0
```

### Пример конфигурации для стабильного детектирования

```python
# В config.py

# Уменьшить количество найденных окружностей
BARBELL_CIRCLE_MIN_DIST = 80
BARBELL_CIRCLE_PARAM2 = 45

# Усилить приоритет стабильности позиции
BARBELL_POSITION_STABILITY_WEIGHT = 0.8
BARBELL_RADIUS_STABILITY_WEIGHT = 0.2

# Усилить штраф за горизонтальные скачки
BARBELL_X_STABILITY_GAIN = 2.5

# Включить фильтрацию выбросов
BARBELL_REJECT_OUTLIERS = True
BARBELL_MAX_JUMP_PIXELS = 80
```

### Быстрая настройка через горячие клавиши

1. Запустите программу с видео файлом
2. Нажмите `d` для включения отладки
3. Нажимайте `s` пока не увидите желтые окружности на штанге
4. Если слишком много окружностей — нажимайте `w` пока не останется нужная
5. Настройте размеры `t`/`y` и `u`/`i` если нужно
6. Скопируйте значения из консоли в `config.py`

![Быстрая настройка](screenshots/quick_tuning.png)
*Процесс быстрой настройки параметров*

---

## Настройка трекинга позы

### Параметры MediaPipe

#### Минимальная уверенность обнаружения

**MIN_DETECTION_CONFIDENCE** (0.0-1.0)

Порог уверенности для обнаружения позы.

**Как настроить**:
- **Увеличить** (0.7-0.8): строже обнаружение, меньше ложных срабатываний
- **Уменьшить** (0.3-0.4): мягче обнаружение, находит позу чаще

**Рекомендация**: Начните с 0.5, уменьшайте если поза не обнаруживается.

#### Минимальная уверенность отслеживания

**MIN_TRACKING_CONFIDENCE** (0.0-1.0)

Порог уверенности для отслеживания позы.

**Как настроить**:
- **Увеличить** (0.7-0.8): более стабильное отслеживание
- **Уменьшить** (0.3-0.4): лучше отслеживает быстрые движения

**Рекомендация**: Начните с 0.5, уменьшайте если поза теряется при быстрых движениях.

### Сложность модели

**MODEL_COMPLEXITY** (0, 1, 2)

Сложность модели MediaPipe Pose.

- **0**: Быстро, менее точно
- **1**: Баланс скорости и точности (рекомендуется)
- **2**: Медленно, наиболее точно

**Рекомендация**: Используйте 1 для большинства случаев.

### Сглаживание landmarks

**SMOOTH_LANDMARKS** (True/False)

Включить/выключить сглаживание ключевых точек.

- **True**: Плавное движение, меньше дрожания
- **False**: Более отзывчивое, но может дрожать

**Рекомендация**: Включите для большинства случаев.

### Настройка визуализации позы

#### Цвета и размеры

Настройте внешний вид скелета в GUI:

- **Цвет костей**: цвет линий скелета (по умолчанию оранжевый)
- **Цвет суставов**: цвет точек суставов (по умолчанию голубой)
- **Толщина костей**: толщина линий (по умолчанию 6)
- **Радиус суставов**: размер точек (по умолчанию 6)

![Настройка визуализации](screenshots/pose_visualization_settings.png)
*Настройка визуализации позы*

#### Слепые зоны

Настройте зоны, где скелет не отображается:

- **Слепая зона X**: доля от ширины (0.2 = 20% с каждой стороны)
- **Слепая зона Y**: доля от высоты (0.2 = 20% сверху и снизу)

**Использование**: Скрывает скелет когда человек выходит за пределы центральной зоны.

---

## UDP данные и интеграция

### Формат данных

Приложение отправляет данные через UDP в формате JSON на порт, указанный в `config.py` (по умолчанию `127.0.0.1:5005`).

#### Полный формат данных

```json
{
  "timestamp": 1234567890.123,
  "barbell": {
    "position": [640, 360],
    "confidence": 0.9,
    "source": "hough"
  },
  "knee_positions": {
    "left_knee": [300, 400],
    "right_knee": [340, 400],
    "left_knee_angle": 90.0,
    "right_knee_angle": 95.0
  },
  "joints": {
    "0": [0.5, 0.3, 0.0],
    "1": [0.52, 0.31, 0.0],
    ...
    "33": [0.6, 0.8, 0.0]
  },
  "barbell_path": [
    {"x": 500.0, "y": 300.0, "timestamp": 1234567890.100},
    {"x": 502.0, "y": 298.0, "timestamp": 1234567890.120}
  ],
  "angles": {
    "left_arm": 120.5,
    "right_arm": 125.3,
    "left_leg": 90.0,
    "right_leg": 95.2
  }
}
```

### Описание полей

#### timestamp

Временная метка в секундах (Unix timestamp с дробной частью).

#### barbell

Данные о текущей позиции штанги:

- **position**: `[x, y]` в пикселях или `null` если не обнаружена
- **confidence**: уверенность обнаружения (0.0-1.0) или `null`
- **source**: источник обнаружения: `"hough"`, `"contour"`, или `"predict"`

#### knee_positions

Данные о коленях:

- **left_knee**: `[x, y]` в пикселях или `null`
- **right_knee**: `[x, y]` в пикселях или `null`
- **left_knee_angle**: угол в градусах или `null`
- **right_knee_angle**: угол в градусах или `null`

#### joints

Все 33 ключевые точки MediaPipe Pose:

- Ключ: индекс точки (0-32)
- Значение: `[x, y, z]` в нормализованных координатах (0.0-1.0)

**Индексы ключевых точек**:
- 0-10: Голова и туловище
- 11-16: Руки
- 23-32: Ноги

#### barbell_path

Массив точек траектории движения штанги:

- **x**: координата X в пикселях
- **y**: координата Y в пикселях
- **timestamp**: временная метка точки

#### angles

Углы суставов в градусах:

- **left_arm**: угол левого локтя
- **right_arm**: угол правого локтя
- **left_leg**: угол левого колена
- **right_leg**: угол правого колена

### Настройка UDP

#### Изменение адреса и порта

Откройте `config.py` и измените:

```python
UDP_HOST = "127.0.0.1"  # IP адрес получателя
UDP_PORT = 5005         # Порт
```

#### Отправка на другой компьютер

Измените `UDP_HOST` на IP адрес целевого компьютера:

```python
UDP_HOST = "192.168.1.100"  # IP адрес другого компьютера
```

### Тестирование UDP

Запустите тестовый получатель:

```bash
python test_udp_receiver.py
```

Затем запустите основное приложение. Тестовый скрипт будет выводить все получаемые данные в консоль.

![Тестирование UDP](screenshots/udp_testing.png)
*Тестирование UDP отправки данных*

### Интеграция с Unreal Engine

1. **Настройте UDP получатель** в Unreal Engine
2. **Укажите порт** в `config.py` (по умолчанию 5005)
3. **Парсите JSON** данные в Blueprint или C++
4. **Используйте данные** для управления анимацией или визуализации

Пример Blueprint:

```
Event BeginPlay
  → Create UDP Socket (Port 5005)
  → Bind Event On Received Data
    → Parse JSON String
    → Extract barbell.position
    → Update Actor Location
```

![Интеграция с Unreal Engine](screenshots/unreal_integration.png)
*Пример интеграции с Unreal Engine*

---

## Дополнительные возможности

### NDI стриминг

Приложение поддерживает стриминг через NDI для использования в OBS, vMix и других приложениях.

#### Настройка NDI

1. **Установите NDI SDK** (если требуется)
2. **Включите NDI** в GUI: отметьте "Использовать NDI"
3. **Укажите имя потока**: введите имя для NDI стрима
4. **Запустите приложение**

![Настройка NDI](screenshots/ndi_setup.png)
*Настройка NDI стриминга*

#### Использование в OBS

1. Запустите приложение с включенным NDI
2. В OBS добавьте источник "NDI Source"
3. Выберите поток с указанным именем

![NDI в OBS](screenshots/ndi_obs.png)
*Использование NDI в OBS Studio*

### Виртуальная камера

Приложение может создавать виртуальную камеру для использования в Zoom, Teams и других приложениях.

#### Настройка виртуальной камеры

1. **Включите виртуальную камеру** в GUI: отметьте "Использовать виртуальную камеру"
2. **Запустите приложение**
3. **Выберите камеру** в приложении для видеозвонков

![Настройка виртуальной камеры](screenshots/virtual_cam_setup.png)
*Настройка виртуальной камеры*

### Поддержка DeckLink карт

Приложение поддерживает профессиональные карты захвата Blackmagic DeckLink через FFmpeg.

#### Настройка DeckLink

1. **Установите FFmpeg** с поддержкой DeckLink
2. **Укажите путь к FFmpeg** в `config.py`:

```python
FFMPEG_PATH = r"C:/ffmpeg/bin/ffmpeg.exe"
```

3. **Используйте источник** `decklink:Имя_Устройства` в GUI

Пример:

```
decklink:DeckLink Mini Recorder 4K
```

Или с параметрами:

```
decklink:DeckLink Mini Recorder 4K?width=1920&height=1080&fps=50
```

![Настройка DeckLink](screenshots/decklink_setup.png)
*Настройка DeckLink карты захвата*

### Автоматическая очистка пути

Приложение автоматически очищает путь штанги при простое.

#### Настройка авто-очистки

В `config.py`:

```python
BARBELL_IDLE_CLEAR_SECONDS = 4.5   # Секунд без движения до очистки
BARBELL_IDLE_MIN_MOVE_PX = 8       # Минимальный сдвиг в пикселях
```

**Как работает**:
- Если штанга не двигается более `BARBELL_IDLE_CLEAR_SECONDS` секунд
- И сдвиг меньше `BARBELL_IDLE_MIN_MOVE_PX` пикселей
- Путь автоматически очищается

---

## Решение проблем

### Штанга не обнаруживается

#### Проблема: Штанга не находится вообще

**Решения**:

1. **Уменьшите param2**:
   - Нажмите `s` несколько раз во время работы
   - Или в `config.py`: `BARBELL_CIRCLE_PARAM2 = 20`

2. **Проверьте размер окружности**:
   - Увеличьте `BARBELL_CIRCLE_MAX_RADIUS` если штанга большая
   - Уменьшите `BARBELL_CIRCLE_MIN_RADIUS` если штанга маленькая
   - Используйте клавиши `t`/`y` и `u`/`i`

3. **Включите режим отладки**:
   - Нажмите `d` для визуализации процесса
   - Проверьте, находятся ли окружности

4. **Проверьте освещение**:
   - Убедитесь, что штанга хорошо видна
   - Избегайте пересветов и теней

![Проблема обнаружения](screenshots/detection_problem.png)
*Режим отладки для диагностики проблем*

#### Проблема: Обнаруживается слишком много ложных окружностей

**Решения**:

1. **Увеличьте param2**:
   - Нажмите `w` несколько раз
   - Или в `config.py`: `BARBELL_CIRCLE_PARAM2 = 40`

2. **Увеличьте param1**:
   - В `config.py`: `BARBELL_CIRCLE_PARAM1 = 100`

3. **Уменьшите область поиска**:
   - В `config.py`: `BARBELL_USE_SEARCH_REGION = True`
   - Это ограничит поиск областью между руками

#### Проблема: Штанга обнаруживается нестабильно (пропадает)

**Решения**:

1. **Уменьшите param2** для нахождения больше окружностей
2. **Увеличьте размытие**:
   - В `config.py`: `BARBELL_BLUR_SIZE = 11`, `BARBELL_BLUR_SIGMA = 3`
3. **Включите сглаживание**:
   - В `config.py`: `BARBELL_SMOOTHING_FACTOR = 0.7`

### Поза не обнаруживается

#### Проблема: Поза не находится

**Решения**:

1. **Уменьшите пороги уверенности**:
   - В GUI: уменьшите "Минимальная уверенность обнаружения" до 0.3-0.4
   - В `config.py`: `MIN_DETECTION_CONFIDENCE = 0.3`

2. **Проверьте положение камеры**:
   - Убедитесь, что человек виден полностью (вид сбоку)
   - Избегайте перекрытий и плохого освещения

3. **Уменьшите сложность модели**:
   - В GUI: выберите "Сложность модели: 0" (быстрее, но менее точно)

![Проблема обнаружения позы](screenshots/pose_detection_problem.png)
*Диагностика проблем обнаружения позы*

#### Проблема: Поза теряется при быстрых движениях

**Решения**:

1. **Уменьшите порог отслеживания**:
   - В GUI: уменьшите "Минимальная уверенность отслеживания" до 0.3-0.4
   - В `config.py`: `MIN_TRACKING_CONFIDENCE = 0.3`

2. **Увеличьте частоту обработки**:
   - В GUI: уменьшите "Обработка каждого N-го кадра" до 1

### Проблемы с производительностью

#### Проблема: Низкий FPS

**Решения**:

1. **Уменьшите разрешение обработки**:
   - В GUI: выберите меньшее разрешение (например, 320x180)

2. **Увеличьте пропуск кадров**:
   - В GUI: увеличьте "Обработка каждого N-го кадра" до 2-3

3. **Отключите ненужные функции**:
   - Отключите трекинг позы если не нужен (клавиша `1`)
   - Отключите визуализацию суставов

4. **Используйте меньшую сложность модели**:
   - В GUI: выберите "Сложность модели: 0"

![Оптимизация производительности](screenshots/performance_optimization.png)
*Настройки для оптимизации производительности*

### Проблемы с камерой

#### Проблема: Камера не открывается

**Решения**:

1. **Проверьте подключение камеры**
2. **Обновите список камер**: нажмите "Обновить камеры" в GUI
3. **Попробуйте другой индекс**: выберите другую камеру из списка
4. **Проверьте драйверы**: убедитесь, что драйверы камеры установлены

#### Проблема: Низкое качество видео

**Решения**:

1. **Проверьте настройки камеры** в системе
2. **Увеличьте разрешение** в `config.py`:
   ```python
   VIDEO_WIDTH = 1920
   VIDEO_HEIGHT = 1080
   ```
3. **Проверьте освещение**: убедитесь, что сцена хорошо освещена

### Проблемы с UDP

#### Проблема: Данные не отправляются

**Решения**:

1. **Проверьте порт**: убедитесь, что порт в `config.py` совпадает с получателем
2. **Проверьте брандмауэр**: разрешите Python через брандмауэр
3. **Проверьте IP адрес**: для отправки на другой компьютер укажите правильный IP
4. **Запустите тестовый получатель**: `python test_udp_receiver.py`

---

## Горячие клавиши

### Основные команды

| Клавиша | Действие |
|---------|----------|
| `q` или `ESC` | Выход / Остановка |
| `c` | Очистить путь штанги вручную |
| `1` | Переключить трекинг позы (вкл/выкл) |
| `2` | Переключить трекинг штанги (вкл/выкл) |
| `d` | Переключить режим отладки штанги |

### Настройка параметров штанги

| Клавиша | Параметр | Действие |
|---------|----------|----------|
| `w` | `param2` | Увеличить (строже детектирование) |
| `s` | `param2` | Уменьшить (мягче детектирование) |
| `e` | `param1` | Увеличить |
| `r` | `param1` | Уменьшить |
| `t` | `minRadius` | Уменьшить |
| `y` | `minRadius` | Увеличить |
| `u` | `maxRadius` | Уменьшить |
| `i` | `maxRadius` | Увеличить |
| `o` | `minDist` | Уменьшить |
| `p` | `minDist` | Увеличить |
| `j` | Размытие | Увеличить |
| `k` | Размытие | Уменьшить |

![Горячие клавиши](screenshots/hotkeys.png)
*Справка по горячим клавишам в приложении*

---

## Технические детали

### Архитектура приложения

Приложение использует многопоточную архитектуру:

1. **Поток захвата** (`CaptureThread`): захватывает кадры с камеры или видео файла
2. **Поток обработки** (`ProcThread`): обрабатывает кадры (MediaPipe + трекинг штанги)
3. **Поток рендеринга** (`_render_loop`): отрисовывает визуализацию и отправляет UDP

![Архитектура приложения](screenshots/architecture.png)
*Архитектура многопоточного приложения*

### Алгоритм обнаружения штанги

1. **Предобработка**:
   - Конвертация в градации серого
   - Применение CLAHE (локальное выравнивание гистограммы)
   - Медианный фильтр
   - Гауссово размытие

2. **Обнаружение окружностей**:
   - Алгоритм HoughCircles для поиска окружностей
   - Fallback на поиск по контурам при провале HoughCircles

3. **Выбор лучшей окружности**:
   - Учет стабильности позиции (близость к предыдущей)
   - Учет стабильности радиуса (похожий размер)
   - Штраф за горизонтальные скачки

4. **Анти-дрожание**:
   - Медианный фильтр для мелких движений
   - 2-tap сглаживание при малой скорости
   - Блокировка по X при низкой скорости

5. **Обновление пути**:
   - Добавление новой точки в путь
   - Автоматическая очистка при простое

![Алгоритм обнаружения](screenshots/detection_algorithm.png)
*Схема алгоритма обнаружения штанги*

### Формат данных MediaPipe

MediaPipe Pose возвращает 33 ключевые точки в нормализованных координатах (0.0-1.0):

- **x, y**: координаты в долях от размера кадра
- **z**: глубина (относительная)
- **visibility**: видимость точки (0.0-1.0)

**Индексы ключевых точек**:
- 0: Нос
- 1-2: Глаза
- 3-4: Уши
- 5-10: Туловище
- 11-16: Руки
- 23-32: Ноги

### Производительность

**Типичная производительность**:

- **Только поза**: 30-60 FPS (зависит от разрешения)
- **Только штанга**: 50-120 FPS
- **Поза + штанга**: 20-50 FPS

**Оптимизация**:

- Уменьшите разрешение обработки для увеличения FPS
- Увеличьте пропуск кадров (обработка каждого N-го кадра)
- Используйте меньшую сложность модели MediaPipe
- Отключите ненужные функции визуализации

---

## Заключение

Это руководство покрывает все основные аспекты использования приложения DartHeavyLifting. Для получения дополнительной помощи:

- Проверьте файлы документации в папке проекта
- Изучите комментарии в `config.py` для детального описания параметров
- Используйте режим отладки (`d`) для диагностики проблем

**Удачных тренировок!** 💪

---

## Приложения

### A. Список всех параметров config.py

Полный список всех параметров конфигурации с описаниями доступен в файле `config.py` в корне проекта.

### B. Примеры использования

#### Пример 1: Базовая настройка для приседаний

```python
# В config.py
BARBELL_CIRCLE_MIN_RADIUS = 50
BARBELL_CIRCLE_MAX_RADIUS = 100
BARBELL_CIRCLE_PARAM2 = 30
MIN_DETECTION_CONFIDENCE = 0.5
MIN_TRACKING_CONFIDENCE = 0.5
```

#### Пример 2: Настройка для жима лежа

```python
# В config.py
BARBELL_CIRCLE_MIN_RADIUS = 40
BARBELL_CIRCLE_MAX_RADIUS = 80
BARBELL_CIRCLE_PARAM2 = 35
BARBELL_USE_SEARCH_REGION = True
```

### C. Часто задаваемые вопросы

**Q: Можно ли запустить несколько экземпляров одновременно?**  
A: Да, можно запустить несколько экземпляров с разными камерами.

**Q: Как изменить UDP порт?**  
A: Измените `UDP_PORT` в `config.py`.

**Q: Поддерживается ли Linux?**  
A: Да, приложение работает на Windows, Linux и macOS.

**Q: Можно ли использовать без камеры?**  
A: Да, можно использовать видео файл для тестирования.

**Q: Как сохранить настройки?**  
A: Настройки сохраняются в `config.py`. Изменения в GUI не сохраняются автоматически (требуется ручное редактирование `config.py`).

---

*Версия руководства: 1.0*  
*Дата обновления: 2024*

