# 🎯 Unified Main - Объединенное приложение

## ✅ Что это?

`unified_main.py` - это **объединенное приложение**, которое включает:
- ✅ **GUI интерфейс** из `main_pose_tracker.py`
- ✅ **Трекинг штанги** из `main.py`
- ✅ **Выбор камеры** через GUI
- ✅ **Выбор режима работы** (только поза, только штанга, или оба)
- ✅ **Полный формат UDP данных** (поза + штанга + путь)

## 🚀 Запуск

```bash
python unified_main.py
```

## 📋 Возможности

### 1. Выбор режима работы
В GUI есть секция **"🎯 Режим работы"** с тремя опциями:
- **Только поза** - отслеживает только позу человека
- **Только штанга** - отслеживает только штангу
- **Поза + штанга** - отслеживает и позу, и штангу (по умолчанию)

### 2. Выбор камеры
В секции **"📷 Источник видео"**:
- Выберите камеру из списка доступных камер
- Или выберите видео файл (MP4/MOV/AVI)
- Кнопка "Обновить камеры" для обновления списка

### 3. Настройки
- **Настройки обработки** - разрешение, частота кадров
- **Внешний вид** - цвета костей/суставов, размеры
- **Настройки модели** - сложность, сглаживание, пороги
- **Выходные потоки** - NDI, виртуальная камера

## 📡 Формат UDP данных

Отправляет полный формат данных на порт из `config.py` (по умолчанию 5005):

```json
{
  "timestamp": 1234567890.123,
  "barbell": {
    "position": [640, 360],
    "confidence": 0.9,
    "source": "hough"
  },
  "knee_positions": {
    "left_knee": [300, 400],
    "right_knee": [340, 400],
    "left_knee_angle": 90.0,
    "right_knee_angle": 95.0
  },
  "joints": {...},
  "barbell_path": [...],
  "angles": {
    "left_arm": 120.5,
    "right_arm": 125.3,
    "left_leg": 90.0,
    "right_leg": 95.2
  }
}
```

## 🎮 Использование

1. **Запустите приложение:**
   ```bash
   python unified_main.py
   ```

2. **Выберите режим работы:**
   - Радиокнопки в секции "🎯 Режим работы"

3. **Выберите камеру:**
   - Выберите камеру из списка или видео файл

4. **Настройте параметры** (опционально):
   - Разрешение обработки
   - Цвета и размеры скелета
   - Параметры модели

5. **Нажмите "▶️ Запуск"**

6. **Остановка:**
   - Кнопка "⏹️ Остановка" в GUI
   - Или нажмите ESC в окне OpenCV

## 📷 Использование разных камер

Можно запустить несколько экземпляров приложения с разными камерами:

```bash
# Окно 1: Камера 0
python unified_main.py
# Выберите камеру 0 в GUI

# Окно 2: Камера 1
python unified_main.py
# Выберите камеру 1 в GUI
```

Каждое окно работает независимо со своей камерой!

## ⚙️ Режимы работы

### Только поза
- Отслеживает позу человека
- Вычисляет углы рук и ног
- Отправляет данные о позе через UDP
- **НЕ отслеживает штангу**

### Только штанга
- Отслеживает только штангу
- Рисует путь штанги
- Отправляет данные о штанге через UDP
- **НЕ отслеживает позу**

### Поза + штанга (рекомендуется)
- Отслеживает и позу, и штангу
- Полный формат UDP данных
- Визуализация скелета и пути штанги
- Использует положение рук для улучшения поиска штанги

## 🔧 Настройки

Все настройки доступны через GUI:
- **Разрешение обработки** - влияет на скорость (меньше = быстрее)
- **Частота кадров** - целевой FPS
- **Цвета и размеры** - настройка внешнего вида скелета
- **Параметры модели** - точность обнаружения позы

## 📊 Преимущества

✅ **Один файл** - все функции в одном приложении  
✅ **GUI интерфейс** - удобная настройка  
✅ **Выбор камеры** - можно использовать разные камеры  
✅ **Выбор режима** - гибкая настройка работы  
✅ **Полный формат данных** - все данные в одном UDP пакете  

## ⚠️ Примечания

- Использует порт из `config.py` (по умолчанию 5005)
- Можно запускать несколько экземпляров с разными камерами
- Режим работы выбирается при запуске (можно изменить перед запуском)

