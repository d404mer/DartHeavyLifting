# Быстрый старт

## Запуск с видео файлом

Самый простой способ - использовать аргумент командной строки:

```bash
python main.py -v "ваше_видео.mp4"
```

### Примеры

```bash
# Видео в той же папке
python main.py -v test.mp4

# Видео в подпапке
python main.py -v videos/workout.mp4

# Видео с абсолютным путем (Windows)
python main.py -v "C:\Videos\barbell.mp4"

# Видео с абсолютным путем (Linux/Mac)
python main.py -v "/home/user/videos/barbell.mp4"
```

### Поддерживаемые форматы

- `.mp4` (рекомендуется)
- `.avi`
- `.mov`
- `.mkv`
- Другие форматы, поддерживаемые OpenCV

## Альтернативный способ

Можно указать путь в файле `config.py`:

1. Откройте `config.py`
2. Найдите строку: `DEBUG_VIDEO_PATH = None`
3. Измените на: `DEBUG_VIDEO_PATH = "путь/к/видео.mp4"`
4. Запустите: `python main.py`

## Запуск с камеры

Если не указан видео файл, будет использоваться камера:

```bash
python main.py
```

Или установите в `config.py`: `DEBUG_VIDEO_PATH = None`

## Получить справку

```bash
python main.py --help
```

## Что вы увидите

1. Окно с видео и наложенной визуализацией:
   - 🟢 Зеленый путь - траектория движения штанги
   - 🔵 Синие точки - колени
   - 🔴 Красные линии - кости ног
   - 📐 Углы коленей отображаются рядом с коленями

2. Данные отправляются через UDP на `127.0.0.1:5005` в формате JSON

3. Логи сохраняются в файл `tracking.log`

## Горячие клавиши

- `q` - выход
- `c` - очистить путь штанги

## Решение проблем

### Видео не открывается
- Проверьте путь к файлу
- Убедитесь, что файл существует
- Проверьте формат видео (рекомендуется .mp4)

### Не обнаруживается поза или штанга
- Убедитесь, что человек виден в кадре (вид сбоку)
- Убедитесь, что штанга видна (кругляшок сбоку)
- Попробуйте изменить параметры в `config.py`:
  - `BARBELL_CIRCLE_MIN_RADIUS` и `BARBELL_CIRCLE_MAX_RADIUS` для размера штанги
  - `MIN_DETECTION_CONFIDENCE` и `MIN_TRACKING_CONFIDENCE` для чувствительности обнаружения позы

